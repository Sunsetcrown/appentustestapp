import 'package:flutter/material.dart';
class Pictureup extends StatelessWidget {
  const Pictureup({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
